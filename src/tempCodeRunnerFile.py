    # print("Even states:", even_states)
    # E_even = torch.tensor([E for E,_ in even_states], device=H.device, dtype=torch.float64)
    # E_odd  = torch.tensor([E for E,_ in odd_states], device=H.device, dtype=torch.float64)
